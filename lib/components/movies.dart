import './rate_movie.dart';
import "package:flutter/material.dart";

class Movies extends StatelessWidget {
  const Movies({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const RateMovie();
  }
}
